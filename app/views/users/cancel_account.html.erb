<div id="change_profile" class="col-md-8 col-md-offset-2">
	<div class="row">
		<h2><%= t('cancel account') %></h2>
	</div>
	<hr>
	<div class="row">
		<div class="col-lg-12" style="text-align:center;">
			<p><%= t('delete your account?') %></p>
			<%= button_to t('yes I do'), registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: 'btn btn-danger' %>
		</div>
	</div>
</div>